{
  "name": "mytestrepo",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && sh test/calculator.test.sh"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
